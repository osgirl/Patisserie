module.exports = {
    name: 'sample',
    description: 'sample',
    execute(message, args) {
        const Discord = require('discord.js');
        const fs = require('fs');
        const parse = require('csv-parser');
        
    }
}